name: iOS IPA Build

on:
  push:
    branches: [Aok]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: kaliOS/package-lock.json

      - name: Install dependencies
        run: cd kaliOS && npm ci

      - name: Install Expo CLI
        run: npm install -g expo

      - name: Expo prebuild iOS
        run: cd kaliOS && npx expo prebuild --platform ios --clean --no-install

      - name: Inject codesign xcconfig (overrides all targets)
        run: |
          cat > /tmp/no-codesign.xcconfig << 'EOF'
          CODE_SIGNING_ALLOWED = NO
          CODE_SIGNING_REQUIRED = NO
          CODE_SIGN_IDENTITY =
          PROVISIONING_PROFILE =
          EXPANDED_CODE_SIGN_IDENTITY =
          EOF

          PODFILE="$GITHUB_WORKSPACE/kaliOS/ios/Podfile"
          if ! grep -q 'no-codesign.xcconfig' "$PODFILE"; then
            # Append xcconfig include at the very top of Podfile
            echo "xcodeproj_config = '/tmp/no-codesign.xcconfig'" | cat - "$PODFILE" > /tmp/pf_tmp && mv /tmp/pf_tmp "$PODFILE"
            echo "xcconfig override injected in Podfile"
          fi

      - name: Pod install
        run: |
          set -o pipefail
          cd "$GITHUB_WORKSPACE/kaliOS/ios"
          pod install --repo-update 2>&1 | tee "$RUNNER_TEMP/pod-install.log"

      - name: Create codesign override xcconfig
        run: |
          cat > /tmp/no-codesign.xcconfig << 'EOF'
          CODE_SIGNING_ALLOWED = NO
          CODE_SIGNING_REQUIRED = NO
          CODE_SIGN_IDENTITY =
          PROVISIONING_PROFILE =
          EXPANDED_CODE_SIGN_IDENTITY =
          EOF
          echo "xcconfig created"

      - name: Build .app unsigned
        run: |
          set -o pipefail
          IOS_DIR="$GITHUB_WORKSPACE/kaliOS/ios"
          BUILD_DIR="$RUNNER_TEMP/build"
          LOG_FILE="$RUNNER_TEMP/xcodebuild.log"
          mkdir -p "$BUILD_DIR"

          WORKSPACE=$(find "$IOS_DIR" -maxdepth 2 -name "*.xcworkspace" \
            -not -name "Pods.xcworkspace" \
            -not -path "*/xcuserdata/*" | head -1)

          if [ -z "$WORKSPACE" ]; then
            echo "ERREUR: aucun .xcworkspace"
            ls -la "$IOS_DIR"
            exit 1
          fi

          SCHEME=$(xcodebuild -list -workspace "$WORKSPACE" 2>/dev/null \
            | awk '/Schemes:/{flag=1;next} /^[[:space:]]*$/{flag=0} flag{gsub(/^[[:space:]]+/,""); print; exit}')
          [ -z "$SCHEME" ] && SCHEME=$(basename "$WORKSPACE" .xcworkspace)

          echo "Workspace : $WORKSPACE"
          echo "Scheme    : $SCHEME"

          xcodebuild build \
            -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -destination "generic/platform=iOS" \
            -derivedDataPath "$BUILD_DIR" \
            -xcconfig /tmp/no-codesign.xcconfig \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            PROVISIONING_PROFILE="" \
            EXPANDED_CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            2>&1 | tee "$LOG_FILE"

          BUILD_EXIT=${PIPESTATUS[0]}
          if [ $BUILD_EXIT -ne 0 ]; then
            echo "===== BUILD ERRORS ====="
            grep "error:" "$LOG_FILE" | grep -v "^note" | tail -80
            exit 1
          fi

          echo "===== Apps trouvees ====="
          find "$BUILD_DIR" -name "*.app" -not -name "*.appex"

      - name: Package .ipa
        run: |
          BUILD_DIR="$RUNNER_TEMP/build"
          APP_PATH=$(find "$BUILD_DIR/Build/Products" -maxdepth 3 \
            -path "*Release-iphoneos*" -name "*.app" \
            -not -name "*.appex" 2>/dev/null | head -1)

          echo "APP: $APP_PATH"
          if [ -z "$APP_PATH" ]; then
            find "$BUILD_DIR" -maxdepth 6
            exit 1
          fi

          mkdir -p "$RUNNER_TEMP/Payload"
          cp -R "$APP_PATH" "$RUNNER_TEMP/Payload/"
          cd "$RUNNER_TEMP"
          zip -r AgonPreview-unsigned.ipa Payload
          ls -lh AgonPreview-unsigned.ipa
          echo "IPA OK!"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: AgonPreview-unsigned-ipa
          path: ${{ runner.temp }}/AgonPreview-unsigned.ipa
          retention-days: 30

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-logs
          path: |
            ${{ runner.temp }}/pod-install.log
            ${{ runner.temp }}/xcodebuild.log
          retention-days: 7
